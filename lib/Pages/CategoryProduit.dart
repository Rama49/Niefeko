import 'package:flutter/material.dart';

class CategoryProduit extends StatelessWidget {
  const CategoryProduit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}